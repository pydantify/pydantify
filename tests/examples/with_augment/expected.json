{
  "$defs": {
    "ConfigurationContainer": {
      "description": "Just a simple example of a container with uses.",
      "properties": {
        "configuration:devicename": {
          "$ref": "#/$defs/DevicenameLeaf"
        },
        "namespaces:namespaces": {
          "default": [],
          "items": {
            "$ref": "#/$defs/NamespacesListEntry"
          },
          "type": "array"
        }
      },
      "required": [
        "configuration:devicename"
      ],
      "title": "ConfigurationContainer",
      "type": "object"
    },
    "DevicenameLeaf": {
      "description": "Device name. Example value: sw01",
      "type": "string"
    },
    "InterfacesListEntry": {
      "description": "List of configured device interfaces",
      "properties": {
        "interfaces:name": {
          "$ref": "#/$defs/NameLeaf2"
        },
        "interfaces:ip": {
          "$ref": "#/$defs/IpLeaf",
          "default": null
        }
      },
      "required": [
        "interfaces:name"
      ],
      "title": "InterfacesListEntry",
      "type": "object"
    },
    "IpLeaf": {
      "description": "Interface IP",
      "type": "string"
    },
    "NameLeaf": {
      "description": "Interface name. Example value: GigabitEthernet 0/0/0",
      "type": "string"
    },
    "NameLeaf2": {
      "description": "Interface name. Example value: GigabitEthernet 0/0/0",
      "type": "string"
    },
    "NamespacesListEntry": {
      "description": "List of configured device namespaces",
      "properties": {
        "namespaces:name": {
          "$ref": "#/$defs/NameLeaf"
        },
        "interfaces:interfaces": {
          "default": [],
          "items": {
            "$ref": "#/$defs/InterfacesListEntry"
          },
          "type": "array"
        }
      },
      "required": [
        "namespaces:name"
      ],
      "title": "NamespacesListEntry",
      "type": "object"
    }
  },
  "description": "Initialize an instance of this class and serialize it to JSON; this results in a RESTCONF payload.\n\n## Tips\nInitialization:\n- all values have to be set via keyword arguments\n- if a class contains only a `root` field, it can be initialized as follows:\n    - `member=MyNode(root=<value>)`\n    - `member=<value>`\n\nSerialziation:\n- `exclude_defaults=True` omits fields set to their default value (recommended)\n- `by_alias=True` ensures qualified names are used (necessary)",
  "properties": {
    "configuration:configuration": {
      "$ref": "#/$defs/ConfigurationContainer",
      "default": null
    }
  },
  "title": "Model",
  "type": "object"
}

